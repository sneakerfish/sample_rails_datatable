== Sample Rails DataTable

This is an example of how to use the jQuery DataTable plug-in with a Ruby on Rails 3.2 application using
AJAX to support the data pagination.

== Rake tasks

There is a rake task included which will add 100 random entries to the contacts table. It uses the faker
gem to generate the fake data.
  
  rake contacts:add_random

